import {Tldr} from "../../framework/components/Tldr"
import {StandardCarousel} from "../../framework/components/Carousel";
import {Link} from "react-router-dom"


import Image1Drive from './drive_images/drives_beckhoff.svg'
import Image2Drive from './drive_images/drives_cannon.svg'
import Image3Drive from './drive_images/drives_delta.svg'
import Image4Drive from './drive_images/drives_invertek.svg'
import Image5Drive from './drive_images/drives_jvl.svg'
import Image6Drive from './drive_images/drives_kollmorgen.svg'
import Image7Drive from './drive_images/drives_nanotec.svg'
import Image8Drive from './drive_images/drives_om.svg'
import Image9Drive from './drive_images/drives_omron.svg'

import Image1Motor from './motor_images/motors_bldc.svg'
import Image2Motor from './motor_images/motors_frameless1.svg'
import Image3Motor from './motor_images/motors_servo.svg'
import Image4Motor from './motor_images/motors_stepper_encoder.svg'
import Image5Motor from './motor_images/motors_frameless2.svg'
import Image6Motor from './motor_images/motors_linear.svg'
import Image7Motor from './motor_images/motors_pancake.svg'
import Image8Motor from './motor_images/motors_hollow_shaft.svg'
import Image9Motor from './motor_images/motors_stepper.svg'

# {props.subtitle}

<Tldr>
- Works with all EtherCAT drives
- Also integrates with embedded driver ICs
- Wide range of drives pre-configured
- Easy to integrate your own drives
</Tldr>

## EtherCAT
If you are using &gbt with an EtherCAT fieldbus, we can support any stepper or BLDC or servo drive that offers a CiA-402 drive profile and cyclic-synchronous-position mode (CSP) – this is almost every EtherCAT drive on the market.

This means you can control motors ranging from tiny 4mm diameter 50,000 rpm BLDC devices through to multi-megawatt motors the size of trucks.

Drives that have been pre-integrated and tested include:

* Oriental Motor - AZD series
* Omron - G5 series (also Panasonic MINAS A5)
* Beckhoff - EL7031, EL7041, EL7037, EL7046, EL7211, EL2522, AX500
* Kollmorgen - AKD series
* Delta - ASDA series
* JVL - MIS series
* Cannon automata - SMC3
* Nanotec - N5
* Trinamic - TMC8461/2




<StandardCarousel height={"500px"} fade={true} dotsOn={false} pauseOnHover={false} imgdata={[
    [`${Image1Drive}`, "image1"],
    [`${Image2Drive}`, "image2"],
    [`${Image3Drive}`, "image3"],
    [`${Image4Drive}`, "image4"],
    [`${Image5Drive}`, "image5"],
    [`${Image6Drive}`, "image6"],
    [`${Image7Drive}`, "image7"],
    [`${Image8Drive}`, "image8"],
    [`${Image9Drive}`, "image9"],
]} />


See <Link to="../docs/gbem/drive_support_overview">EtherCAT pre-configured drive overview</Link> for a full list of the pre-configured drives.




## Embedded stepper / servo / BLDC

If you are embedding a machine control, fieldbus based drives may not be suitable. For example, if your application is a control for a machine containing 3 or 4 stepper motors and a small set of digital and analog IO, you may well want to embed the drives into yoru control electronics.

Part of the &gbt is &gbsm which is a software application for use on Linux (usually embedded Linux) or microcontroller platforms. This works with motor drive ICs to control stepper / BDLC or servo motors in an embedded environment.



<StandardCarousel height={"300px"} fade={true} dotsTop={'480px'} dotsOn={false} labelsOn={true} pauseOnHover={false} imgdata={[
    [`${Image1Motor}`, "BDLC motors"],
    [`${Image2Motor}`, "Frameless motors"],
    [`${Image3Motor}`, "Servo motors"],
    [`${Image4Motor}`, "Closed loop stepper motors"],
    [`${Image5Motor}`, "Frameless motors"],
    [`${Image6Motor}`, "Linear actuators"],
    [`${Image7Motor}`, "Pancake motors"],
    [`${Image8Motor}`, "Hollow shaft motors"],
    [`${Image9Motor}`, "Ordinary bipolar stepper motors"],
]} />







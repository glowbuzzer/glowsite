
import reactIcon from "./react-icon.png?glowsite"
export const heroImage=reactIcon
import {TitleImage} from "../../../framework/components/TitleImage"
import {PageTag} from "../../../framework/components/PageTags"
import {ReadingTime} from "../../../framework/components/ReadingTime"

# {props.subtitle}

<ReadingTime time="5"/>
<PageTag tags={props.tags}/>

## Introduction

<TitleImage image={reactIcon} alt="React logo"/>

React Native is describes itself as "combining the best parts of native development with React with a best-in-class JavaScript library for building user interfaces

It allows you to build native applications for Android and iOS using React.

This is interesting to us as it allows a way to use the &gbt to build HMI (user interface) applications to control machines that can can be deployed to tablets/phones/HMI etc.

This blog post discusses how to use the &gbr components with React Native.

## Expo


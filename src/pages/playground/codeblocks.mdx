import {Markdown} from "../../framework/components/Markdown";

# Basic codeblock

This is `inline code`.

```tsx
import React from "react"
import {Button} from "antd"

type Foo={
    basic: number
    bar(param1: number): void
}

function something() {
}

const x:number[]=[42, 54]
const y=something()

export default () => <Button>Hello</Button>
```

# Codeblock with entities

To install &gbc; enter the following commands
```sh
wget https://downloads.glowbuzzer.com/releases/linux-amd64/&gbc_version;/gbc.tgz
```

# Codeblock with demo
```jsx demo
import React from "react"
import {Button} from "antd"

export default () => <Button>Hello</Button>
```
Nodes in-between
- bullet
- bullet

# Codeblock with very long line

```jsx
export default () => <Button>xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx</Button>
```

# Glowbuzzer control

```jsx demo
import React from "react"
import {BitFieldDisplay} from "@glowbuzzer/controls/dro"

export default () => <BitFieldDisplay value={42} bitCount={16}/>
```

```jsx demo
import React from "react"
import {CartesianDro} from "@glowbuzzer/controls/dro"

export default () => <CartesianDro/>
```

# Codeblock inside JSX tags

<div>
    This is inside jsx
    - Bullet 1
    - Bullet 2

    ```js
    function test() {
        console.log('test');
    }
    ```
</div>

# Using ReactMarkdown

<Markdown children={"```js\nfunction test() {\n  console.log('hello')\n}\n```"}/>
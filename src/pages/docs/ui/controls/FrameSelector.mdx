import {GithubSourceLink} from "../../../../framework/components";

# FrameSelector

<GithubSourceLink project="gbr" lib="controls" path="misc/FrameSelector.tsx"/>

Provides a way to select preferred frame of reference (normally used in combination with a digital readout component).

```jsx demo
import {FrameSelector} from "@glowbuzzer/controls";

export default () => {
    function onChange(v) {
        console.log("Frame changed: ", v);
    }

    return <FrameSelector onChange={onChange} defaultFrame={1}/>;
}
```


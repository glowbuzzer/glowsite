# {props.subtitle}

## Redux in &gbr

We use redux in &gbr. Redux is an Open Source library widely used to manage application state. It provides a predictable container for storing the application's state.



In sample apps configured for you
If you want to configure redux this is what you need to do


autogen slices docs
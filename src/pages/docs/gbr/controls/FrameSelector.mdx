```jsx demo
import React from "react"
import {FrameSelector} from "@glowbuzzer/controls";

export default () => {
    function onChange(v) {
        console.log("Frame changed: ", v);
    }

    return <div><FrameSelector onChange={onChange} defaultFrame={1}/></div>;
}
```


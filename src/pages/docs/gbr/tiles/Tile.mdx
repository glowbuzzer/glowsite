```jsx demo small
import React from "react"
import {Tile, TileSettings} from "@glowbuzzer/controls"
import {Button} from "antd"

export default () => {
    function save_settings() {
        alert("Save the settings!")
    }
    return <Tile title="Example Tile" help="Optional help text in title bar"
                 controls={<Button>Optional Controls</Button>} footer="Optional footer"
                 settings={<TileSettings onConfirm={save_settings}>Settings go here</TileSettings>}>

            <div>This is the content of the tile</div>

    </Tile>
}
```

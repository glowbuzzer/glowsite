# ToolPathTile

Displays the toolpath and preview in three dimensions.

```jsx demo toolpath
import React from "react"
import {ToolPathTile} from "@glowbuzzer/controls";

export default () => <ToolPathTile/>
```


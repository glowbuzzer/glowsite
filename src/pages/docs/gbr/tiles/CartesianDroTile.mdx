```jsx demo tile tall
import React from "react"
import {DockTileDefinitionBuilder, CartesianDroTileDefinition, CartesianDroTile} from "@glowbuzzer/controls";
import {DockExampleLayout} from "@glowsite";

const definition=DockTileDefinitionBuilder(CartesianDroTileDefinition)
    .render(() => <CartesianDroTile clipboardMode={true} />)
    .build();

export default () => <DockExampleLayout tile={definition}/>
```

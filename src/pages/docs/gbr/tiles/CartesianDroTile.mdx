```jsx demo tile tall
import React from "react"
import {GlowbuzzerTileDefinitions, DockTileDefinitionBuilder, CartesianDroTile} from "@glowbuzzer/controls";
import {DockExampleLayout} from "@glowsite";

const definition=DockTileDefinitionBuilder(GlowbuzzerTileDefinitions.CARTESIAN_DRO)
    .render(() => <CartesianDroTile clipboardMode={true} />)
    .build();

export default () => <DockExampleLayout tile={definition}/>
```

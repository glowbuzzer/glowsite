import {GithubSourceLink} from "../../../../framework/components";

# TaskDisplay

<GithubSourceLink project="gbr" lib="controls" path="toolpath/ToolPathTile.tsx"/>

Displays tasks status and provides basic controls for starting, cancelling and restarting tasks.

```jsx demo tasks
import React from "react"
import {TasksTile} from "@glowbuzzer/controls";

export default () => <TasksTile/>
```

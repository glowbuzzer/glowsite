import {Tldr} from "../../../framework/components/Tldr";
import { Link } from "react-router-dom"

# {props.subtitle}

## Raspberry Pi
<Tldr>
    - Needs pigpio library installing
    - Clone &gbsm from GitHub
</Tldr>

### pipgio

pigpio is a library for the Raspberry Pi which allows control of the General Purpose Input Outputs (GPIO) pins.

pigpio is available on GitHub at: <a href="https://github.com/joan2937/pigpio">https://github.com/joan2937/pigpio</a>

To install:

````shell
wget https://github.com/joan2937/pigpio/archive/master.zip
unzip master.zip
cd pigpio-master
make
sudo make install

````

### &gbsm

```shell
git clone --recursive https://github.com/glowbuzzer/gbsm
```


This will clone the project and the sub-modules it depends on into gbsm directory.

```shell
mkdir build
cd build
cmake .. -DFLAVOUR=PI
make

```
This will create the $gbsm binary in the `builds` directory that can be run with `./GBSM`.

See <Link to="/docs/gbsm/running">the documentation here</Link> for more details on how to run &gbsm.



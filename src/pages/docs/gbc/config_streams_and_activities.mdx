import {Tldr} from "../../../framework/components/Tldr"
import { Link } from "react-router-dom"

# {props.subtitle}

## Introduction

The stream section of the config is brief a

the soloActivity section of teh config maps solo activities to kcs

## Details




## Example JSON

```json
 "stream": {
        "default": {
        }
    },
```

```json
"soloActivity": {
        "default": {
        },
        "aux": {
        }
    },
```


## Link to schema documentation
`stream` maps to `StreamConfig` - see <Link to="/docs/gbc/schema/StreamConfig">here for the schema documentation</Link>

`soloActivity` maps to `SoloActivityConfig` - see <Link to="/docs/gbc/schema/SoloActivityConfig">here for the schema documentation</Link>

Download GBEM from GitHub with:
`git clone https://gitgub.com/glowbuzzer/gbem`
And `cd` into the directory.

Then, either import the code into your IDE and build there or from the command line:

```shell
mkdir build
cd build
cmake ..
make
```

